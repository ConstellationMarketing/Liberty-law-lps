[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Form submission settings - redirect to custom thank you page
[forms]
  redirect_to = "/thank-you"
